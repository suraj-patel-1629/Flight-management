server:
  port: 8082

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/suraj-patel-1629/MicroServiceConfigServer
          clone-on-start: true

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
